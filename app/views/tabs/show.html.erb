<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @tab.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @tab.description %>
</p>

<%= link_to 'Edit', edit_binder_tab_path(@binder, @tab) %> |
<%= link_to 'Back', binder_tabs_path(@binder) %>
